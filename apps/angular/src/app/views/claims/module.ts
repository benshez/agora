import { NgModule } from '@angular/core';
import { COMPONENT_MODULES, COMPONENT_DECLARATIONS } from '@views/claims/common';

@NgModule({
    imports: [...COMPONENT_MODULES],
    declarations: [...COMPONENT_DECLARATIONS]
})
export class ClaimsModule { }
