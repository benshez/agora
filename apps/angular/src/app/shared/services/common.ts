import { ProgressBarService } from '@shared/services/bar/service';
import { LoggerService } from '@shared/services/logger/service';

export const CORE_SERVICES: Array<any> = [ProgressBarService, LoggerService];
