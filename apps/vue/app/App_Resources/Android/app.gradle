// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "org.nativescript.agora"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
